{
  "name": "tnbf-website",
  "version": "1.0.0",
  "description": "TNBF Business Loan Website",
  "main": "api/index.js",
  "scripts": {
    "build": "node -e \"const fs=require('fs'); const path=require('path'); const root=process.cwd(); const out=path.join(root,'public'); fs.rmSync(out,{recursive:true,force:true}); fs.mkdirSync(out,{recursive:true}); const copyDir=(srcRel,dstRel)=>{const src=path.join(root,srcRel); if(!fs.existsSync(src)) return; fs.cpSync(src,path.join(out,dstRel??srcRel),{recursive:true});}; const copyFile=(srcRel,dstRel)=>{const src=path.join(root,srcRel); if(!fs.existsSync(src)) return; fs.mkdirSync(path.dirname(path.join(out,dstRel??srcRel)),{recursive:true}); fs.cpSync(src,path.join(out,dstRel??srcRel));}; copyDir('assets'); copyDir('seo'); copyDir('blog'); copyDir('forms'); copyFile('robots.txt'); copyFile('sitemap.xml'); const pagesRoot=path.join(root,'pages'); if(fs.existsSync(pagesRoot)) { for(const ent of fs.readdirSync(pagesRoot,{withFileTypes:true})) { if(ent.isDirectory()) { if(ent.name==='home') { copyFile('pages/home/index.html','index.html'); continue; } if(ent.name==='contact-apply') { copyDir('pages/contact-apply','apply-now'); continue; } copyDir('pages/'+ent.name, ent.name); } else if(ent.isFile() && ent.name.toLowerCase()==='thanks.html') { copyFile('pages/thanks.html','thanks.html'); } } } console.log('Built static output to', out);\"",
    "start": "node backend/server.js",
    "dev": "nodemon backend/server.js"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "nodemailer": "^6.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": "22.x"
  }
}
